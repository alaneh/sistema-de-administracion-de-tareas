mixin crearLista (lista = {})
    form.agregar-proyecto(action=`/nueva-lista/${lista.id || ''}` method ="POST")
        .campo 
            label(for="nombre") Nombre de la lista: 
            input(value=lista.nombre type="text" id="nombre" name="nombre" placeholder ="Nombre de la lista")
        .campo.enviar 
            input(type="submit" value=`${lista.nombre ? 'Guardar' : 'Agregar'}` class="boton")